
<%= render "partials/navbar" %>

<div class='col-sm-10 col-sm-offset-1 col-md-8 col-md-offset-2 col-lg-6 col-lg-offset-3'>

      <div class="font2 col-sm-6 col-sm-offset-2">
            <h2>Sign In</h2>
      </div><br><br><br><br>

    <%= form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: 'form-horizontal'}) do |f| %>

        <div class="form-group">
            <div class="col-sm-6">
                <%= f.email_field :email, autofocus: true , class: "font3 darkGray form-control", :placeholder => 'E-mail Address' %>
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-6">
                <%= f.password_field :password, autocomplete: "off", class: "font3 darkGray form-control", :placeholder => 'Password' %>
            </div>
        </div>

        <% if devise_mapping.rememberable? -%>
        <div class="form-group">
            <div class="font3 midGray col-sm-6 col-sm-offset-2">
                <%= f.check_box :remember_me %> <%= f.label :remember_me %>
            </div>
        </div>
        <% end -%>

        <div class="form-group">
            <div class="col-sm-6 col-sm-offset-2">
                <%= f.submit "Sign in", class: 'btn btn-primary' %>
            </div>
        </div>

        <div class="col-sm-6 col-sm-offset-2">
            <%= render "devise/shared/links" %>
        </div>


    <% end %>

</div>

